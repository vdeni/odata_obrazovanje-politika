```{r}
library(here)
library(magrittr)
library(dplyr)
library(readr)
```

```{r load-data}
d_visoko <- readr::read_csv2(here::here('data',
                                        'popis_visoko-obrazovanje.csv')) %>%
    janitor::clean_names(.)
```

